{% set name = "dascore" %}
{% set version = "0.1.21" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/dascore-{{ version }}.tar.gz
  sha256: 0a2de7f4ea94fa9eb4165fe01ebe579093d7162a75476b234cc2250d55aae9e9

build:
  noarch: python
  script: SETUPTOOLS_SCM_PRETEND_VERSION={{ version }} {{ PYTHON }} -m pip install . -v
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools >=45
    - setuptools_scm >=3.4
    - toml
  run:
    - matplotlib-base >=3.8
    - numpy >=1.24
    - packaging
    - pandas >=2.0
    - pooch >=1.2
    - pydantic >2.1
    - rich
    - scipy >=1.15.0
    - pytables >=3.7
    - typing_extensions >=4.12
    - pint
    - python >={{ python_min }}
    - numba
    - h5py
    - bottleneck

test:
  imports:
    - dascore
  commands:
    - pip check
    - python -c "import dascore; ver = dascore.__version__; assert ver != '0.0.0'"
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/dascore/
  summary: A python library distributed fiber optic sensing
  license: LGPL-3.0-or-later
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - d-chambers
    - eileenrmartin
    - ahmadtourei
